# Northwind Internal

The internal monorepo for the Northwind team. It holds the small HTTP service that
powers our status page under `services/api/`, alongside the company documents the
operations team maintains — the employee handbook and the current-quarter budget —
under `company/`. Code and company records live in one workspace so a single agent
can work across both without leaving the repository.
