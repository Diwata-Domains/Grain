# Tooling Notes

Lightweight inbox for workflow friction, tool bugs, or observations noticed mid-session.
Agents write here; user reviews and escalates to the appropriate tracker.

| Date | Type | Command | Observation | Severity | Status |
|------|------|---------|-------------|----------|--------|
| 2026-04-21 | workflow_friction | `grain workflow next` | Active task with implementation recorded in `results.md` still routes to `task_execute` while the intended workflow is Execute -> Review -> Close; review is not being surfaced as the next legal step once execution artifacts exist. | medium | fixed — P31-T01 TASK-0204: `stale_task_pointer` stop reason now surfaced instead of silent reset |
| 2026-04-21 | tool_bug | `grain workflow run` / `grain task create` | Packet ID allocation reused `TASK-0001` for Phase 16 because `next_task_id()` only scans active directories under `tasks/` and ignores archived packets under `tasks/archive/`, so bare task IDs are not globally monotonic after archiving. | medium | fixed — already resolved in current code; `next_task_id()` uses rglob and scans archives |
| 2026-06-11 | ux | `grain workflow next --format json` | `--format` flag is a global option and must come before the subcommand: `grain --format json workflow next`. Placing it after the subcommand returns "No such option: --format". The spec documents (v0.4.0 enforcement_spec, cli_ergonomics_spec) all reference `grain workflow next --format json` — these are wrong and should use global flag order. | medium | fixed — P31-T01 TASK-0204: all runtime docs and canonical specs updated; cli_spec.md §4.3 note added |
| 2026-06-11 | missing-command | `grain notes add` | `grain notes add` does not exist yet (v0.4.0 planned in T09/feedback_spec). Agents cannot log tooling friction via CLI — must edit tooling_notes.md directly. This breaks the agent-agnostic logging pattern that T09 is designed to fix. | high | open |
| 2026-06-11 | tool_bug | `grain workflow next` / packet validator | `validate_packet` requires all 4 files (task.md, context.md, plan.md, deliverable_spec.md) for every non-done packet, even tasks created with `--simple` or tasks that are purely docs/spec work. Simple packets created with `grain task create --simple` only have task.md + results.md, meaning they always fail validation when in_progress. Workaround: stub the 3 missing files with one-line content. Fix needed: packet should carry a `simple: true` metadata flag (or infer from absent planning files) so the validator skips the planning-file requirement for simple packets. | high | fixed — P31-T01 TASK-0204: `validate_packet_files` infers simple packet from task.md + no planning files |
| 2026-06-11 | ux | packet structure | Working docs created as separate `docs/working/` files (e.g. scaffold_audit.md, apply_graduation.md) during Phase 30 could have lived inside the task packet as `plan.md` (spec/design decisions) and `deliverable_spec.md` (what the output looks like). Externalizing them creates drift between the packet and the spec. Better pattern: put the reasoning and spec detail in the packet's `plan.md` and `deliverable_spec.md`; only promote to `docs/canonical/` or `docs/working/` when the artifact is meant to outlive the task and be referenced by other tasks or agents. | medium | open |
