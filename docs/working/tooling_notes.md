# Tooling Notes

Lightweight inbox for workflow friction, tool bugs, or observations noticed mid-session.
Agents write here; user reviews and escalates to the appropriate tracker.

| ID | Date | Type | Command | Observation | Severity | Status |
|----|------|------|---------|-------------|----------|--------|
| 1 | 2026-04-21 | workflow_friction | `grain workflow next` | Active task with implementation recorded in `results.md` still routes to `task_execute` while the intended workflow is Execute -> Review -> Close; review is not being surfaced as the next legal step once execution artifacts exist. | medium | fixed — p31-t01 task-0204: `stale_task_pointer` stop reason now surfaced instead of silent reset |
| 2 | 2026-04-21 | tool_bug | `grain workflow run` / `grain task create` | Packet ID allocation reused `TASK-0001` for Phase 16 because `next_task_id()` only scans active directories under `tasks/` and ignores archived packets under `tasks/archive/`, so bare task IDs are not globally monotonic after archiving. | medium | fixed — already resolved in current code; `next_task_id()` uses rglob and scans archives |
| 3 | 2026-06-11 | ux | `grain workflow next --format json` | `--format` flag is a global option and must come before the subcommand: `grain --format json workflow next`. Placing it after the subcommand returns "No such option: --format". The spec documents (v0.4.0 enforcement_spec, cli_ergonomics_spec) all reference `grain workflow next --format json` — these are wrong and should use global flag order. | medium | fixed — p31-t01 task-0204: all runtime docs and canonical specs updated; cli_spec.md §4.3 note added |
| 4 | 2026-06-11 | missing-command | `grain notes add` | `grain notes add` does not exist yet (v0.4.0 planned in T09/feedback_spec). Agents cannot log tooling friction via CLI — must edit tooling_notes.md directly. This breaks the agent-agnostic logging pattern that T09 is designed to fix. | high | open |
| 5 | 2026-06-11 | tool_bug | `grain workflow next` / packet validator | `validate_packet` requires all 4 files (task.md, context.md, plan.md, deliverable_spec.md) for every non-done packet, even tasks created with `--simple` or tasks that are purely docs/spec work. Simple packets created with `grain task create --simple` only have task.md + results.md, meaning they always fail validation when in_progress. Workaround: stub the 3 missing files with one-line content. Fix needed: packet should carry a `simple: true` metadata flag (or infer from absent planning files) so the validator skips the planning-file requirement for simple packets. | high | fixed — p31-t01 task-0204: `validate_packet_files` infers simple packet from task.md + no planning files |
| 6 | 2026-06-11 | ux | packet structure | Working docs created as separate `docs/working/` files (e.g. scaffold_audit.md, apply_graduation.md) during Phase 30 could have lived inside the task packet as `plan.md` (spec/design decisions) and `deliverable_spec.md` (what the output looks like). Externalizing them creates drift between the packet and the spec. Better pattern: put the reasoning and spec detail in the packet's `plan.md` and `deliverable_spec.md`; only promote to `docs/canonical/` or `docs/working/` when the artifact is meant to outlive the task and be referenced by other tasks or agents. | medium | open |
| 7 | 2026-07-09 | bug | grain workflow reconcile --fix | reconcile reports missing_packet for work that genuinely shipped without a packet (Phase 38, commits c917cb7 + f63716d) and --fix cannot repair it: it only syncs backlog status and the current_task.md pointer. There is no way to express 'executed without a packet' — the warning is permanent noise, or you fabricate 11 retroactive packets. Needs an explicit annotation (e.g. status: done (no packet, see <commit>)) that reconcile accepts. | high | open |
| 8 | 2026-07-09 | bug | grain phase list | Phases 21-31 carry the closure marker inside the heading text ('## Phase 21 — ... ✓ CLOSED') rather than a status field, so phase list reports them [open] while the closed-phase ledger in current_focus.md calls them closed. Two sources of truth for phase closure; the parser only reads one. Related: P40-T11 (was P36-T16) DRYs the phase-heading regex. | medium | open |
| 9 | 2026-07-09 | friction | grain phase close | Closing a phase whose remaining tasks are drafts requires hand-moving every draft to another phase first, then hand-writing a workflow_metrics.md section, or close refuses. Phase 35 hit the empty-phase variant and earned --allow-empty; Phase 36 hit the drafts variant and had 12 tasks moved by hand. A first-class 'defer remaining tasks to phase N' operation would have made both one command. | medium | open |
| 10 | 2026-07-09 | bug | trace release / release-python.yml | grain-kit 0.6.0 now depends on grain-contracts>=0.1, which is NOT on PyPI. release-python.yml publishes grain-kit only; a 0.6.0 release would ship an uninstallable package. grain-contracts must be published FIRST, and the release workflow needs a second publish target. Also: products/grain is subtree-mirrored to a public repo where packages/grain-contracts does not exist, so the mirror cannot resolve the dep from source — only from PyPI. | high | open |
| 11 | 2026-07-09 | bug | grain workflow reconcile --fix | reconcile --fix advertises 'Auto-repair safe drift (backlog status sync, current_task.md stale pointer)' but packet_backlog_mismatch reports fix_available:false, so the one drift its help names first is not repairable. Reproduced: grain task create + grain task start writes in_progress into the packet, backlog entry authored separately says draft, --fix is a no-op. Either implement the sync or stop advertising it. | medium | open |
