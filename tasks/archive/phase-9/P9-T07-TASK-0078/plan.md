# Plan: TASK-0078

## Approach

Implement a focused validator for OrchestratorPlan artifact dictionaries and cover it with unit and integration tests. Integration coverage will invoke stable CLI command surfaces (`adapter list/show`, `orchestrate scope/plan`) and validate real proposal artifacts against known adapter IDs.

---

## Step 1 — Add Validator Module

Create `src/grain/validators/orchestrator_validator.py` with a `validate_orchestrator_plan_dict(...)` function implementing contract minimums from `data_contracts.md §18.3`.

---

## Step 2 — Add Validator Unit Tests

Create `tests/test_orchestrator_validator.py` for:
- valid plan payload
- missing/invalid `plan_id`
- invalid `status`
- missing candidate required fields
- unknown active adapter IDs

---

## Step 3 — Add Cross-Command Integration Tests

Create `tests/test_orchestration_integration.py` to verify:
- adapter IDs from `adapter list` can validate an artifact generated by `orchestrate plan`
- `adapter show` and `orchestrate scope --adapter` align on adapter identity

---

## Verification

- `.venv/bin/pytest -q tests/test_orchestrator_validator.py tests/test_orchestration_integration.py tests/test_orchestrate_cmd.py tests/test_adapter_cmd.py`
- `.venv/bin/grain docs validate`
- `.venv/bin/grain task validate --id TASK-0078`
- `.venv/bin/pytest -q`
