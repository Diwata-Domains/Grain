# Deliverable Spec: TASK-0050

## Required Deliverables
- A new integration test file covering the Phase 5 core command chain.
- Coverage for init, docs validation, task creation, context export, and review commands.
- Focused and full-suite validation results recorded in `results.md`.

## Acceptance Criteria
- The integration test must pass against the real CLI.
- The test should use real filesystem artifacts and not mocks for the core flow.
- The coverage should remain narrow and deterministic.
