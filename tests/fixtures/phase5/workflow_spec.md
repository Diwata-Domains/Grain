# Workflow Spec

Forge uses packet-scoped work with explicit review and handoff steps.

## Expectations

- Commands remain filesystem-local.
- Review-ready packets move through `review` before closure.
- Handoff artifacts should stay deterministic and reviewable.
